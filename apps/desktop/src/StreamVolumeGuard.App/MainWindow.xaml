<Window x:Class="StreamVolumeGuard.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="StreamVolume Guard Hub Desktop" Icon="Assets/app.ico" Height="820" Width="1180" MinHeight="720" MinWidth="980"
        Background="{DynamicResource AppBackgroundBrush}" Foreground="{DynamicResource TextBrush}">
    <Window.Resources>
        <SolidColorBrush x:Key="AppBackgroundBrush" Color="#F7F8FA" />
        <SolidColorBrush x:Key="HeaderBrush" Color="#10202C" />
        <SolidColorBrush x:Key="PanelBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="PanelSoftBrush" Color="#F5F8FA" />
        <SolidColorBrush x:Key="BorderBrushSoft" Color="#D7E1E8" />
        <SolidColorBrush x:Key="TextBrush" Color="#17202A" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#60707D" />
        <SolidColorBrush x:Key="AccentBrush" Color="#1F6F78" />
        <SolidColorBrush x:Key="TableAltBrush" Color="#F5F8FA" />
        <SolidColorBrush x:Key="SafeBrush" Color="#188A4D" />
        <SolidColorBrush x:Key="WarnBrush" Color="#D88211" />
        <SolidColorBrush x:Key="DangerBrush" Color="#C73333" />

        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Padding" Value="14" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Background" Value="{DynamicResource PanelBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style x:Key="SummaryCardStyle" TargetType="Border" BasedOn="{StaticResource PanelStyle}">
            <Setter Property="Padding" Value="12" />
            <Setter Property="Background" Value="{DynamicResource PanelSoftBrush}" />
        </Style>

        <Style x:Key="TrustBadgeStyle" TargetType="Border">
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Background" Value="#EDF8F1" />
            <Setter Property="BorderBrush" Value="#CFE2D6" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="0,0,6,0" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="12,5" />
            <Setter Property="BorderBrush" Value="#C8D0D8" />
            <Setter Property="Background" Value="{DynamicResource PanelBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style x:Key="HeaderButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#183141" />
            <Setter Property="BorderBrush" Value="#365263" />
            <Setter Property="Foreground" Value="#F4F8FB" />
        </Style>

        <Style x:Key="DebugButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="MinHeight" Value="28" />
            <Setter Property="Padding" Value="9,3" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Margin" Value="6,2,0,2" />
        </Style>

        <Style x:Key="PanicButtonStyle" TargetType="Button" BasedOn="{StaticResource HeaderButtonStyle}">
            <Setter Property="Background" Value="#B82828" />
            <Setter Property="BorderBrush" Value="#B82828" />
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="MinWidth" Value="92" />
        </Style>

        <Style x:Key="TargetPresetButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Style.Triggers>
                <Trigger Property="Tag" Value="Active">
                    <Setter Property="Background" Value="{DynamicResource SafeBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource SafeBrush}" />
                    <Setter Property="Foreground" Value="#FFFFFF" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#F4F8FB" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="Background" Value="{DynamicResource PanelBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="RowBackground" Value="{DynamicResource PanelBrush}" />
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource TableAltBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}" />
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource BorderBrushSoft}" />
            <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource BorderBrushSoft}" />
            <Setter Property="RowHeight" Value="38" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource PanelSoftBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Padding" Value="8,7" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}" />
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        </Style>
    </Window.Resources>

    <DockPanel Background="{DynamicResource AppBackgroundBrush}">
        <Border DockPanel.Dock="Top" Background="{DynamicResource HeaderBrush}" Padding="22,18">
            <DockPanel LastChildFill="False">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="40" Height="40" CornerRadius="8" Background="#F4FAFB" Margin="0,0,12,0">
                        <Image Source="Assets/app.ico" Width="28" Height="28" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel>
                        <TextBlock Text="StreamVolume Guard Hub Desktop" Foreground="#F4F8FB" FontSize="21" FontWeight="Bold" />
                        <TextBlock Text="{DynamicResource SubtitleText}" Foreground="#AEC1CC" FontSize="12" FontWeight="SemiBold" Margin="0,3,0,0" />
                    </StackPanel>
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <CheckBox x:Name="AutoEnabledCheckBox" Content="{DynamicResource AutoEnabledText}" Margin="0,0,12,0" Checked="AutoEnabled_Changed" Unchecked="AutoEnabled_Changed" />
                    <CheckBox x:Name="StreamSafeCheckBox" Content="{DynamicResource StreamSafeText}" Margin="0,0,12,0" Checked="StreamSafe_Changed" Unchecked="StreamSafe_Changed" />
                    <Button Content="{Binding ThemeButtonText}" Style="{StaticResource HeaderButtonStyle}" Margin="0,0,8,0" Click="Theme_Click" />
                    <Button Content="{DynamicResource RefreshText}" Style="{StaticResource HeaderButtonStyle}" Margin="0,0,8,0" Click="Refresh_Click" />
                    <Button Content="{DynamicResource PanicText}" Style="{StaticResource PanicButtonStyle}" Click="Panic_Click" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="2*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Border Style="{StaticResource TrustBadgeStyle}">
                        <TextBlock Text="{DynamicResource TrustLocalText}" Foreground="#0D6832" FontSize="11" FontWeight="Black" />
                    </Border>
                    <Border Style="{StaticResource TrustBadgeStyle}">
                        <TextBlock Text="{DynamicResource TrustOpenSourceText}" Foreground="#0D6832" FontSize="11" FontWeight="Black" />
                    </Border>
                    <Border Style="{StaticResource TrustBadgeStyle}">
                        <TextBlock Text="{DynamicResource TrustNoTrackingText}" Foreground="#0D6832" FontSize="11" FontWeight="Black" />
                    </Border>
                    <Border Style="{StaticResource TrustBadgeStyle}">
                        <TextBlock Text="{DynamicResource TrustNoAccountText}" Foreground="#0D6832" FontSize="11" FontWeight="Black" />
                    </Border>
                </StackPanel>
                <TextBlock DockPanel.Dock="Right" Text="{Binding ThemeStatusText}" Foreground="{DynamicResource MutedTextBrush}" FontWeight="SemiBold" VerticalAlignment="Center" />
            </DockPanel>

            <Grid Grid.Row="1" Margin="0,0,0,14">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource SummaryCardStyle}" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource WindowsSourcesTitleText}" Foreground="{DynamicResource AccentBrush}" FontSize="11" FontWeight="Black" />
                            <TextBlock Text="{Binding WindowSourceCountText}" FontSize="18" FontWeight="Bold" Margin="0,4,0,0" />
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="1" Style="{StaticResource SummaryCardStyle}" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource BrowserSourcesTitleText}" Foreground="{DynamicResource AccentBrush}" FontSize="11" FontWeight="Black" />
                            <TextBlock Text="{Binding BrowserSourceCountText}" FontSize="18" FontWeight="Bold" Margin="0,4,0,0" />
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="2" Style="{StaticResource SummaryCardStyle}" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource CoverageTitleText}" Foreground="{DynamicResource AccentBrush}" FontSize="11" FontWeight="Black" />
                            <TextBlock Text="{Binding CoverageScoreText}" FontSize="15" FontWeight="Bold" Margin="0,4,0,0" TextWrapping="Wrap" />
                            <TextBlock Text="{Binding CoverageDetailText}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" Margin="0,2,0,0" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="3" Style="{StaticResource SummaryCardStyle}" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource WatchTitleText}" Foreground="{DynamicResource WarnBrush}" FontSize="11" FontWeight="Black" />
                            <TextBlock Text="{Binding WatchCountText}" FontSize="18" FontWeight="Bold" Margin="0,4,0,0" />
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="4" Style="{StaticResource SummaryCardStyle}">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource ModeTitleText}" Foreground="{DynamicResource AccentBrush}" FontSize="11" FontWeight="Black" />
                            <TextBlock Text="{Binding ModeSummaryText}" FontSize="18" FontWeight="Bold" Margin="0,4,0,0" />
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Grid.Row="1" Style="{StaticResource SummaryCardStyle}" Margin="0,8,0,0" Padding="10">
                    <DockPanel LastChildFill="True">
                        <TextBlock Text="{DynamicResource ExtensionLinkLabelText}" Foreground="{DynamicResource AccentBrush}" FontSize="11" FontWeight="Black" Margin="0,0,12,0" VerticalAlignment="Center" />
                        <TextBlock x:Name="ExtensionLinkStatusText" Text="{Binding ExtensionLinkText}" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" TextWrapping="Wrap" VerticalAlignment="Center" />
                    </DockPanel>
                </Border>
            </Grid>

            <Border Grid.Row="2" Style="{StaticResource PanelStyle}" Margin="0,0,0,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.25*" />
                        <ColumnDefinition Width="0.8*" />
                        <ColumnDefinition Width="0.95*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,14,0">
                        <TextBlock Text="{DynamicResource GlobalOutputTitleText}" FontSize="15" FontWeight="Bold" />
                        <TextBlock Text="{Binding GlobalOutputDeviceText}" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" TextTrimming="CharacterEllipsis" Margin="0,4,0,0" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,14,0">
                        <TextBlock Text="{DynamicResource StateTitleText}" Foreground="{DynamicResource AccentBrush}" FontSize="11" FontWeight="Black" />
                        <TextBlock Text="{Binding GlobalOutputStateText}" FontSize="18" FontWeight="Bold" Margin="0,4,0,0" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="0,0,14,0">
                        <TextBlock Text="{DynamicResource CurrentLevelTitleText}" Foreground="{DynamicResource AccentBrush}" FontSize="11" FontWeight="Black" />
                        <TextBlock Text="{Binding GlobalOutputLevelText}" FontSize="16" FontWeight="Bold" Margin="0,4,0,0" />
                        <TextBlock Text="{Binding GlobalOutputPeakText}" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" Margin="0,2,0,0" />
                    </StackPanel>

                    <TextBlock Grid.Column="3" Text="{Binding GlobalOutputInfoText}" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" TextWrapping="Wrap" VerticalAlignment="Center" />
                </Grid>
            </Border>

            <Border Grid.Row="3" Style="{StaticResource PanelStyle}" Margin="0,0,0,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.2*" />
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="{DynamicResource TargetVolumeTitleText}" FontSize="15" FontWeight="Bold" />
                        <TextBlock Text="{Binding TargetDisplayText}" Foreground="{DynamicResource AccentBrush}" FontSize="18" FontWeight="Black" Margin="0,4,0,0" />
                        <TextBlock Text="{Binding TargetBridgeText}" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,4,14,0" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Button x:Name="TargetQuietButton" Content="{DynamicResource TargetQuietText}" Style="{StaticResource TargetPresetButtonStyle}" MinWidth="82" Margin="0,0,8,0" Click="TargetQuiet_Click" />
                            <Button x:Name="TargetStandardButton" Content="{DynamicResource TargetStandardText}" Style="{StaticResource TargetPresetButtonStyle}" MinWidth="92" Margin="0,0,8,0" Click="TargetStandard_Click" />
                            <Button x:Name="TargetLoudButton" Content="{DynamicResource TargetLoudText}" Style="{StaticResource TargetPresetButtonStyle}" MinWidth="72" Click="TargetLoud_Click" />
                        </StackPanel>
                        <DockPanel>
                            <TextBlock Text="15%" Width="54" Foreground="{DynamicResource MutedTextBrush}" VerticalAlignment="Center" />
                            <TextBlock Text="100%" DockPanel.Dock="Right" Width="54" TextAlignment="Right" Foreground="{DynamicResource MutedTextBrush}" VerticalAlignment="Center" />
                            <Slider x:Name="TargetSlider"
                                    Minimum="-30"
                                    Maximum="-15"
                                    Value="-18"
                                    TickFrequency="0.5"
                                    IsSnapToTickEnabled="True"
                                    SmallChange="0.5"
                                    LargeChange="1"
                                    ValueChanged="TargetSlider_ValueChanged" />
                        </DockPanel>
                    </StackPanel>

                    <TextBlock Grid.Column="2" Text="{DynamicResource TargetScopeText}" Foreground="{DynamicResource AccentBrush}" FontWeight="Black" VerticalAlignment="Center" Margin="18,0,0,0" />
                </Grid>
            </Border>

            <Border Grid.Row="4" Style="{StaticResource PanelStyle}" Margin="0,0,0,14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <DockPanel Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource WindowsAppsTitleText}" FontSize="16" FontWeight="Bold" />
                            <TextBlock Text="{DynamicResource WindowsAppsDescriptionText}" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" Margin="0,3,0,0" />
                        </StackPanel>
                    </DockPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Sessions}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{DynamicResource SourceHeaderText}" Binding="{Binding DisplayName}" Width="2*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource StatusHeaderText}" Binding="{Binding Status}" Width="*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource PeakHeaderText}" Binding="{Binding PeakPercent}" Width="0.8*" IsReadOnly="True" />
                            <DataGridTemplateColumn Header="{DynamicResource VolumeHeaderText}" Width="2*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <DockPanel Margin="0,0,6,0">
                                            <TextBlock Text="{Binding VolumePercent}" Width="48" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}" />
                                            <Slider Minimum="0" Maximum="1" TickFrequency="0.05" Value="{Binding VolumeScalar, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        </DockPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="{DynamicResource ControlHeaderText}" Binding="{Binding ControlSurface}" Width="1.2*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource ControllableHeaderText}" Binding="{Binding IsControllable}" Width="0.9*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource CoverageHeaderText}" Binding="{Binding CoverageStatus}" Width="1.1*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource CoverageActionHeaderText}" Binding="{Binding CoverageAction}" Width="1.5*" IsReadOnly="True" />
                            <DataGridTemplateColumn Header="{DynamicResource ExcludedHeaderText}" Width="0.7*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsExcluded, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  Focusable="False"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Foreground="{DynamicResource TextBrush}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="{DynamicResource ActionHeaderText}" Binding="{Binding LastAction}" Width="2*" IsReadOnly="True" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Row="5" Style="{StaticResource PanelStyle}" Margin="0,0,0,14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="{DynamicResource BrowserSourcesTitleText}" FontSize="16" FontWeight="Bold" />
                        <TextBlock Text="{DynamicResource BrowserAppsDescriptionText}" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" Margin="0,3,0,0" />
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding BrowserSources}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{DynamicResource BrowserHeaderText}" Binding="{Binding BrowserProcess}" Width="*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource SiteHeaderText}" Binding="{Binding SiteName}" Width="*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource StatusHeaderText}" Binding="{Binding Status}" Width="*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource LevelHeaderText}" Binding="{Binding CurrentLevelPercent}" Width="0.8*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource GainHeaderText}" Binding="{Binding AppliedGainPercent}" Width="0.8*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource CalibrationHeaderText}" Binding="{Binding Calibration}" Width="1.1*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource ControlHeaderText}" Binding="{Binding ControlSurface}" Width="1.1*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource ControllableHeaderText}" Binding="{Binding IsControllable}" Width="0.8*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource CoverageHeaderText}" Binding="{Binding CoverageStatus}" Width="1.1*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource CoverageActionHeaderText}" Binding="{Binding CoverageAction}" Width="1.5*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource ReasonHeaderText}" Binding="{Binding BrowserIssueReason}" Width="1.5*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource ActionHeaderText}" Binding="{Binding RecoveryAction}" Width="1.8*" IsReadOnly="True" />
                            <DataGridTextColumn Header="{DynamicResource TitleHeaderText}" Binding="{Binding Title}" Width="1.6*" IsReadOnly="True" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Row="6" Style="{StaticResource PanelStyle}" Padding="10,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Margin="0,0,14,0" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{DynamicResource DebugTitleText}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0" />
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock x:Name="LogStatusText" Text="{DynamicResource LogStatusReadyText}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" TextTrimming="CharacterEllipsis" />
                            <TextBlock x:Name="BridgeStatusText" Text="{DynamicResource BridgePendingStartText}" Foreground="{DynamicResource AccentBrush}" FontSize="11" TextTrimming="CharacterEllipsis" Margin="0,1,0,0" />
                            <TextBlock Text="{Binding GuidedTestStatusText}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" TextTrimming="CharacterEllipsis" Margin="0,1,0,0" />
                        </StackPanel>
                    </Grid>

                    <WrapPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button Content="{DynamicResource GuidedStartText}" Style="{StaticResource DebugButtonStyle}" ToolTip="{DynamicResource GuidedStartTooltipText}" Click="StartGuidedTest_Click" />
                        <Button Content="{DynamicResource GuidedNextText}" Style="{StaticResource DebugButtonStyle}" ToolTip="{DynamicResource GuidedNextTooltipText}" Click="NextGuidedTestStep_Click" />
                        <Button Content="{DynamicResource NewTestText}" Style="{StaticResource DebugButtonStyle}" ToolTip="{DynamicResource NewTestTooltipText}" Click="NewTestSession_Click" />
                        <Button Content="{DynamicResource MarkStepText}" Style="{StaticResource DebugButtonStyle}" ToolTip="{DynamicResource MarkStepTooltipText}" Click="MarkStep_Click" />
                        <Button Content="{DynamicResource CopyLogsText}" Style="{StaticResource DebugButtonStyle}" ToolTip="{DynamicResource CopyLogsTooltipText}" Click="CopyRecentLogs_Click" />
                        <Button Content="{DynamicResource OpenLogsText}" Style="{StaticResource DebugButtonStyle}" ToolTip="{DynamicResource OpenLogsTooltipText}" Click="OpenLogs_Click" />
                        <Button Content="{DynamicResource ObsGuideText}" Style="{StaticResource DebugButtonStyle}" ToolTip="{DynamicResource ObsGuideTooltipText}" Click="OpenObsGuide_Click" />
                        <Button Content="{DynamicResource SimulateText}" Style="{StaticResource DebugButtonStyle}" ToolTip="{DynamicResource SimulateTooltipText}" Click="SimulateBrowserSource_Click" />
                    </WrapPanel>
                </Grid>
            </Border>
        </Grid>
    </DockPanel>
</Window>
